<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myapp.team.cart.mapper.PaymentMapper">
    <resultMap id="PaymentResultMap" type="com.myapp.team.cart.model.Payment">
        <id property="paymentNo" column="payment_no" />
        <result property="ordersId" column="orders_id" />
        <result property="paymentMethod" column="payment_method" />
        <result property="paymentAmount" column="payment_amount" />
        <result property="paymentDate" column="payment_date" />
    </resultMap>

    <select id="findAll" resultMap="PaymentResultMap">
        SELECT * FROM PAYMENT
    </select>

    <select id="findById" parameterType="long" resultMap="PaymentResultMap">
        SELECT * FROM PAYMENT WHERE payment_no = #{paymentNo}
    </select>

    <insert id="insert" parameterType="com.myapp.team.cart.model.Payment">
        INSERT INTO PAYMENT (orders_id, payment_method, payment_amount, payment_date)
        VALUES (#{ordersId}, #{paymentMethod}, #{paymentAmount}, #{paymentDate})
    </insert>

    <update id="update" parameterType="com.myapp.team.cart.model.Payment">
        UPDATE PAYMENT SET
                           orders_id = #{ordersId},
                           payment_method = #{paymentMethod},
                           payment_amount = #{paymentAmount},
                           payment_date = #{paymentDate}
        WHERE payment_no = #{paymentNo}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM PAYMENT WHERE payment_no = #{paymentNo}
    </delete>
</mapper>
